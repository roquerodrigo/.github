# .github

Default community health files for [@roquerodrigo](https://github.com/roquerodrigo)'s repositories.

GitHub falls back to the files in this repository for any repository that does not define its own. Today that is `FUNDING.yml`, which enables the **Sponsor** button.
